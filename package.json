{
	"name": "obsidian-auto-glossary",
	"version": "1.0.2",
	"description": "Obsidian plugin to automatically create and organize indexes (MOCs), glossaries with note embeds, or combined overview notes from folders.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"lint": "eslint src/**/*.ts test/**/*.ts",
		"lint:fix": "eslint src/**/*.ts test/**/*.ts --fix",
		"test": "node -r ./test/setup.cjs --import tsx --test ./test/*.ts",
		"test:coverage": "c8 node -r ./test/setup.cjs --import tsx --test ./test/*.ts",
		"test:only": "node -r ./test/setup.cjs --import tsx --test --test-only ./test/*.ts"
	},
	"keywords": [
		"obsidian",
		"obsidian-plugin",
		"glossary",
		"index",
		"moc"
	],
	"author": "Ennio Italiano",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/ennioitaliano/obsidian-auto-glossary"
	},
	"devDependencies": {
		"@types/node": "^20.17.24",
		"@types/sinon": "^17.0.4",
		"@typescript-eslint/eslint-plugin": "^8.69.0",
		"@typescript-eslint/parser": "^8.69.0",
		"c8": "^12.0.0",
		"esbuild": "^0.25.4",
		"obsidian": "^1.7.2",
		"sinon": "^18.0.1",
		"tslib": "^2.8.1",
		"tsx": "^4.19.4",
		"typescript": "^5.4.5"
	}
}
